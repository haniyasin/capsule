This project is complex tools, module's launchers and modules for creating a abstraction which called the capsule.
For each supported platform was itself own set tools and neccesary parts for creating a abstraction.
Now, we support two platforms - browser, nodejs, but in far future was planned support for android, GNU/Linux, Windows and javame. Abstraction is created with keeping all these plantforms on mind.

What is capsule abstraction? It is not so ease to answer. 
One side is envelopement with api, for execution code. One envelopement and one api for the any supported platforms. You can write application, which, for example, is retriving list all you loved songs in your favorite cloud, if that cloud is having http api. Then, application is extracting some song by your entered criteria and return new playlist for using your favorite player. Of couse, this is example and application may be more complex. It is may to include player, song library, support for many cloud api and many more. What is important? That is this application may be deploed in browser or nodejs OR browser and nodejs divert some modules(gui, user logic, audio etc) to browser and some modules(strorring data, retriving data from clouds etc) to nodejs. Finally, we trying create support for each platform to bring maximum its capabilities. If platform support all capabilities what application need then application may be runned for this platform without back hellping(for example - execution an application in the browser without helping from web server).
Other side is part of dsa and base for new type application. It is applications which is consisted from many services distributed on many devices and platforms. Its is not applications, it is united services which is doing some work together bringing to user experience which one was needed. Above example may be divided into parts: in browser(ui, little logic), in browsers other people(distributed storring songs, playlist etc), in nodejs other people(distributed search songs, storring songs texts). All parts is connected between itself and united in something like application.